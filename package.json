{
  "name": "@edgeguideab/expect",
  "version": "6.1.3",
  "description": "Check for user input in a consistent way and generate error messages for missings ",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [
    "expect",
    "input",
    "arguments",
    "validation"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/edgeguide/expect.git"
  },
  "homepage": "https://github.com/edgeguide/expect",
  "author": "EdgeGuide AB",
  "license": "MIT",
  "scripts": {
    "lint": "tslint --project .",
    "test": "jest",
    "test-d": "node --inspect-brk node_modules/.bin/jest --runInBand",
    "build": "rm -r dist/* && npm run test && npm run lint && tsc && rm dist/definitions.js",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "xregexp": "^4.2.4"
  },
  "devDependencies": {
    "@types/jest": "^23.3.12",
    "@types/node": "^10.12.18",
    "@types/xregexp": "^3.0.29",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.1",
    "typescript": "^3.2.2"
  }
}
